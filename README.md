# 아크릴 응원봉 시안 자동 생성 서비스

AI를 활용하여 고객 요청에 맞는 아크릴 응원봉 시안을 자동으로 생성하는 서비스입니다.

## 🎯 주요 기능

- **기본형 등록**: 제품의 기본형 이미지와 설명 등록
- **레퍼런스 등록**: 실제 제작된 제품 이미지를 레퍼런스로 등록
- **시안 자동 생성**: 로고, 이미지, 텍스트를 입력받아 AI로 시안 생성

## 🛠️ 기술 스택

### 백엔드
- **Node.js** + **TypeScript**
- **Express.js** - REST API 서버
- **Prisma** + **SQLite** - 데이터베이스
- **Google Gemini API** - AI 이미지 생성
- **Multer** - 파일 업로드 처리

### 프론트엔드
- **Next.js 16** + **TypeScript**
- **React 19**
- **Tailwind CSS** - 스타일링 (애플 스타일)

## 📦 설치 및 실행

### 1. 의존성 설치

```bash
# 루트 디렉토리
npm install

# 프론트엔드
cd frontend
npm install
cd ..
```

### 2. 데이터베이스 초기화

```bash
# Prisma 클라이언트 생성
npm run db:generate

# 데이터베이스 스키마 생성
npm run db:push
```

### 3. 환경 변수 설정

`.env` 파일이 이미 설정되어 있습니다. 필요시 수정하세요.

```env
GOOGLE_API_KEY=AIzaSyAv5g9VVup8yqQtxA0-VFql1kEiIaBzsIM
MODEL_NAME=gemini-3-pro-image-preview
PORT=3001
```

### 4. 서버 실행

```bash
# 백엔드 서버 실행 (포트 3001)
npm run dev

# 새 터미널에서 프론트엔드 실행 (포트 3000)
cd frontend
npm run dev
```

### 5. 접속

- 프론트엔드: http://localhost:4000
- 백엔드 API: http://localhost:3001/api

## 📁 프로젝트 구조

```
mockup/
├── prisma/
│   └── schema.prisma          # 데이터베이스 스키마
├── src/
│   ├── config/
│   │   └── config.ts          # 설정 파일
│   ├── controllers/           # API 컨트롤러
│   ├── services/              # 비즈니스 로직
│   ├── routes/                # API 라우트
│   ├── middleware/            # 미들웨어
│   ├── utils/                 # 유틸리티
│   └── server.ts              # 서버 진입점
├── frontend/
│   ├── app/                   # Next.js 앱 라우터
│   │   ├── page.tsx           # 메인 페이지
│   │   ├── base-products/     # 기본형 관리
│   │   ├── references/        # 레퍼런스 관리
│   │   └── designs/           # 시안 생성
│   └── lib/
│       └── api.ts             # API 클라이언트
├── uploads/                   # 업로드된 파일 저장소
│   ├── base-products/
│   ├── references/
│   ├── logos/
│   ├── user-images/
│   └── generated/
└── config/
    └── model-info.json        # 모델 정보
```

## 🔌 API 엔드포인트

### 기본형 (Base Products)
- `GET /api/base-products` - 목록 조회
- `GET /api/base-products/:id` - 상세 조회
- `POST /api/base-products` - 생성 (multipart/form-data)
- `DELETE /api/base-products/:id` - 삭제

### 레퍼런스 (References)
- `GET /api/references` - 목록 조회
- `GET /api/references/:id` - 상세 조회
- `POST /api/references` - 생성 (multipart/form-data)
- `DELETE /api/references/:id` - 삭제

### 시안 (Designs)
- `GET /api/designs` - 목록 조회
- `GET /api/designs/:id` - 상세 조회
- `POST /api/designs` - 생성 (multipart/form-data)
- `DELETE /api/designs/:id` - 삭제

## 🎨 사용 방법

### 1. 기본형 등록
1. 메인 페이지에서 "기본형 등록" 클릭
2. 이름, 설명, 이미지 업로드
3. 등록 완료

### 2. 레퍼런스 등록
1. "레퍼런스 관리" 페이지로 이동
2. 기본형 선택
3. 레퍼런스 이미지 업로드
4. 등록 완료

### 3. 시안 생성
1. "시안 생성" 페이지로 이동
2. 기본형 선택
3. 레퍼런스 선택 (여러 개 가능)
4. 로고, 이미지, 텍스트 입력
5. "시안 생성하기" 클릭
6. AI가 시안 생성 (비동기 처리)

## 📝 모델 정보

- **모델명**: `gemini-3-pro-image-preview`
- **입력 토큰 한도**: 65,536
- **출력 토큰 한도**: 32,768
- **지원 기능**: 이미지 생성, Batch API

## ⚠️ 주의사항

1. **파일 저장**: 업로드된 파일은 `uploads/` 폴더에 저장됩니다.
2. **데이터베이스**: SQLite를 사용하므로 `prisma/dev.db` 파일이 생성됩니다.
3. **API 키**: 프로덕션 환경에서는 환경 변수로 관리하세요.
4. **이미지 크기**: 업로드 파일 크기 제한은 10MB입니다.

## 🚀 개발 스크립트

```bash
# 백엔드 개발 모드
npm run dev

# 프론트엔드 개발 모드
cd frontend && npm run dev

# 데이터베이스 스튜디오 (GUI)
npm run db:studio

# 빌드
npm run build
cd frontend && npm run build
```

## 📄 라이선스

ISC
