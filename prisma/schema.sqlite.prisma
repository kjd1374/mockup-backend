// 로컬 개발용 SQLite 스키마 (백업)
// 프로덕션에서는 PostgreSQL을 사용하므로 이 파일은 참고용입니다.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

// 나머지 모델은 schema.prisma와 동일

